import React from 'react'
import FormTitle from './FormTitle'
import { SectorType } from '../type/SectorType'

export default function SectorList({num, nom} : SectorType) {
  return (
    <div>
        <FormTitle text='Liste des secteurs'/>
        <select>

        </select>
    </div>
  )
}
